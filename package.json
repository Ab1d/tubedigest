{
  "name": "tubedigest-local",
  "version": "1.0.0",
  "description": "TubeDigest Local — Fully local YouTube video summarizer with Ollama AI",
  "scripts": {
    "setup": "bash setup.sh",
    "start:local": "bash start.sh",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": ["youtube", "summarizer", "ollama", "local-ai", "productivity"],
  "license": "MIT"
}
